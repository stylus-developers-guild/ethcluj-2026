[package]
name = "cluj-lending"
version = "0.1.0"
edition = "2024"

[dependencies]
stylus-sdk = "0.10.5"
alloy-sol-types = ">=0.0.0"

[features]
contract-client-gen = []
export-abi = ["stylus-sdk/export-abi"]
